# Smoothing configuration for wrist position filtering.
# Maintained pipeline: interpolate missing frames, apply centered moving
# average, then clamp velocity. Later experimental smoothing variants were
# removed because factory002_middle_phaseB was the more accurate run.

window: 7
max_speed: 2.0
